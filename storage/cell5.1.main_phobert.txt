# =====================================================================
# CELL 5: CẤU HÌNH KIẾN TRÚC MẠNG NƠ-RON VÀ BỘ HUẤN LUYỆN (TRAINER)
# Mục tiêu: Đóng gói dữ liệu PyTorch Dataset, khởi tạo PhoBERT Sequence Classification
# =====================================================================
import os
os.environ["HF_HUB_DISABLE_SYMLINKS_WARNING"] = "1" 

from transformers import AutoModelForSequenceClassification

# Thêm tham số force_download=True để ép tải lại
print("Đang ép tải lại mô hình từ Hugging Face (Vui lòng đợi mạng ổn định)...")
model = AutoModelForSequenceClassification.from_pretrained(
    "vinai/phobert-base-v2", 
    num_labels=num_labels,
    force_download=True 
)
print("Tải thành công!")